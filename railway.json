{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "node",
    "buildCommand": "npm install --workspaces && npm run build --workspace=frontend && cd backend && npm install"
  },
  "deploy": {
    "root": "backend",
    "startCommand": "npm start"
  }
}
